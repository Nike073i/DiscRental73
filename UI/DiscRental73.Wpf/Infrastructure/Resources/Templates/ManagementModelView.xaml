<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:views="clr-namespace:DiscRental73.Wpf.Views"
                    xmlns:mvm="clr-namespace:DiscRental73.Wpf.ViewModels.ManagementViewModels">
    <DataTemplate DataType="{x:Type mvm:BluRayDiscManagementViewModel}">
        <views:ManagementView DataContext="{Binding}"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type mvm:CdDiscManagementViewModel}">
        <views:ManagementView DataContext="{Binding}"/>
    </DataTemplate>
    <DataTemplate DataType="{x:Type mvm:DvdDiscManagementViewModel}">
        <views:ManagementView DataContext="{Binding}"/>
    </DataTemplate>
</ResourceDictionary>