<UserControl x:Class="DiscRental73.Wpf.Views.ManagementView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 mc:Ignorable="d">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" MinWidth="200"/>
            <ColumnDefinition MinWidth ="200"/>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Column="0" Content="{Binding ActionViewModel}"/>
        <ContentControl Grid.Column="1" Content="{Binding EntityViewModel}"/>
        <GridSplitter Grid.Column="0" Width="3" Margin="-3,0"/>
    </Grid>
</UserControl>